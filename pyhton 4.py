// temp

temp= []

for i in range(10);
     temp = float(input(f"enter temperature {i=1}: "))
      temp.append(temp)
     average = sum(temp) / len(temp)
     print(f"\nAverage temperature: {average:.2f} ")